after cloning the repo 
go to the current directory by running cd <directory name>

then use <npm install> in the terminal to install all the required packages into our node app
update the api key of yours from openai.com in .env file
then run <node app.js> or <npm start> to run our server 

you will get a message in the console that your serve is <listening>

now you can use your server 